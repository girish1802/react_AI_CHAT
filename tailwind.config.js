/** @type {import('tailwindcss').Config} */
export default {
  // Use the `.dark` class on <html> or <body> to enable dark mode
  
  content: [
    './index.html',
    './src/**/*.{js,jsx,ts,tsx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
